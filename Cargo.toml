[workspace]
members = ["conductor", "client"]
resolver = "2"

[workspace.dependencies]
tokio = { version = "1", features = ["full"] }
nix = { version = "0.29", features = [
    "fs", "ioctl", "process", "signal", "term", "socket", "user", "net",
] }
libc = "0.2"
